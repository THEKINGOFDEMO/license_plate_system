# 实验计划

## 文档目的

本文件用于记录车牌检测与识别系统的训练、评估与实验安排。

当前阶段为仓库初始化，尚未开始真实训练，因此这里只保留计划结构，不填写虚构指标。

## 实验路线

```text
CCPD 数据准备
  -> YOLO 检测训练与验证
  -> CRNN 识别训练与评估
  -> 检测与识别流水线联调
  -> Django + Vue 系统演示
```

## 阶段规划

### 1. 数据准备

- 解析 CCPD 文件名中的标注信息；
- 生成 YOLO 检测标签；
- 根据检测框裁剪车牌图像，生成 CRNN 识别数据；
- 固定随机种子划分 train/val/test；
- 输出转换日志。

### 2. YOLO 检测实验

计划内容：

- 模型：`YOLOv8n` 或 `YOLOv8s`
- 数据：`CCPD2019` 蓝牌为主
- 环境：`AutoDL + NVIDIA GPU`

需保留的真实产物：

- `best.pt`
- `results.png`
- `PR_curve.png`
- `confusion_matrix.png`
- `val_batch_pred.jpg`
- 检测示例图

需记录的真实指标：

- Precision
- Recall
- mAP@0.5
- mAP@0.5:0.95

TODO：

- 补充真实训练命令
- 补充真实训练日志路径
- 补充真实实验结果表格

### 3. CRNN 识别实验

计划内容：

- 框架：PyTorch
- 模型：CRNN
- 损失函数：CTC Loss

需记录的真实指标：

- 字符准确率
- 整牌准确率

TODO：

- 补充字符集说明
- 补充真实训练与评估命令
- 补充真实 checkpoint 路径与指标

### 4. 系统联调与演示

目标流程：

```text
输入图片 -> YOLO 检测 -> 车牌裁剪 -> CRNN 识别 -> JSON 返回 -> 可视化展示
```

TODO：

- 补充接口联调记录
- 补充前后端联调截图
- 补充失败样例与异常处理记录

## 实验记录要求

- 所有结果必须来自真实运行；
- 没有真实日志时，不填写具体数值；
- 缺失内容统一用 `TODO` 标记，待后续补充。
